name: compose-oci

on:
  push:
    branches:
      - '**'
    tags:
      - '**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  compose-oci:
    uses: openads-project/openads-dev-environment/.github/workflows/compose-oci.yml@main
    secrets: inherit

